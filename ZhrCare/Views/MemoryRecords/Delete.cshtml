@model ZhrCare.Models.MemoryRecord
@{ ViewData["Title"] = "Delete Memory"; }

<div class="zhr-page-wrapper">
    <div class="container text-center">
        <h1 class="zhr-hero-title text-danger">Delete Memory?</h1>
        <p class="zhr-subtitle">This memory will be removed permanently.</p>

        <div class="zhr-card mx-auto text-start" style="max-width: 500px; border-top: 5px solid var(--zhr-accent);">
            <div class="text-center mb-4">
                @if (!string.IsNullOrEmpty(Model.ImagePath)) {
                    <img src="~/uploads/images/@Model.ImagePath" class="rounded mb-3 w-100" style="max-height: 200px; object-fit: cover;" />
                }
                <h5 class="fw-bold">"@Model.Caption"</h5>
            </div>

            <form asp-action="Delete">
                <input type="hidden" asp-for="Id" />
                <button type="submit" class="btn btn-zhr-danger w-100 mb-3">Confirm Permanent Delete</button>
                <div class="text-center">
                    <a asp-action="Index" asp-route-patientId="@Model.PatientId" class="text-muted text-decoration-none small">Go back to gallery</a>
                </div>
            </form>
        </div>
    </div>
</div>